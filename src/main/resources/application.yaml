server:
  port: 8080

spring:
  application:
    name: contacts
  datasource:
    url: jdbc:mysql://localhost:3306/contacts_db?createDatabaseIfNotExist=true
    username: root
    password: admin123
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          format_sql: true